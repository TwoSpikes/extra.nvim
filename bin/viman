#!/bin/env sh

if command -v "nvim" > /dev/null 2>&1; then
	editor="nvim"
elif command -v "vim" > /dev/null 2>&1; then
	editor="vim"
fi
${editor} --cmd "let g:DO_NOT_OPEN_ANYTHING=v:true" -c "set nocul nocuc stal=0 ls=0 sloc=last smd ru" -c "au VimEnter * Man $1 $2|silent only"
